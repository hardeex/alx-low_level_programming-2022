#include "main.h"

/**
 * A function that prints 10 times the alphated in lower case
 */

void print_alphabet_x10(void)
{
	int count= 1;
	char letter;

	while(count <=10)
	{
		letter='a'
			while (letter <='z')
			{
				_putchar(letter);
				letter++
	}
		_putchar('\n')
			count++;

}
