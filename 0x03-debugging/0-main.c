#include "main.h"

/**
 * main- testing the function that prints if a number is postive or negative
 * Return: 0 upon successful execution
 */
int main(void)
{
	int n = 0;

	positive_or_negative(n);
	return (0);
}
